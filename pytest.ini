[pytest]
markers =
    auth:     Basic Authentication enforcement tests
    contract: OpenAPI schema and response contract validation tests
    tenant:   Multi-tenant segregation and isolation tests
    load:     Performance and load tests

addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html
    -m "not load"

testpaths = tests
